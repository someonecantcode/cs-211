# iterator notes

* Used to iterate over java collections framework.

* Operates with methods similar to scanner.

```java
Iterator it = DATASTRUCTURE.iterator();
// Iterator<type>

while(it.hasNext()){
    System.out.println(it.next());
}
```
