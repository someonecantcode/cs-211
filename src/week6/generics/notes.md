# Generics

Allowing for variable types.

```java
public class test<T, U> {

}

```

In total, these are all of the operations we can apply to a class.
```java
private class className<T> extends superClass implements interface1 {
    
}
```